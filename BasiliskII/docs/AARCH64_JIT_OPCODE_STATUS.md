# ARM64 JIT Opcode Status — 64-bit Pointer Safety

## Summary

This is the historical **64-bit pointer-safety** matrix, not a whole-engine
semantic closure verdict. A row marked safe here means only that the reviewed
`get_n_addr()` pointer-truncation pattern is absent or repaired. The independent,
source-derived structural classifications are in
`AARCH64_JIT_CLOSURE_INVENTORY.md`; entries still marked `unreviewed` there are
not promoted by this table.

The ARM64 JIT register allocator is 32-bit. When `get_n_addr()` / `jnf_MEM_GETADR_OFF()`
produces a 64-bit host pointer and caches it in a virtual register, the allocator may
evict it as 32 bits, **truncating the pointer**. Any subsequent memory access through
the truncated pointer writes to the wrong address.

**Fixed pattern**: replace `get_n_addr` + `mov_l_rR`/`mov_l_Rr` (unsafe cached pointer)
with `readlong`/`writelong`/`readword`/`writeword` (reconstructs pointer each time).

## Opcode Family Status

| Family | Instructions | Handlers | Status | Risk | Notes |
|--------|-------------|----------|--------|------|-------|
| 0x0 | ORI/ANDI/SUBI/ADDI/EORI/CMPI/BTST/BCHG/BCLR/BSET | 232 | ✅ Safe | None | No get_n_addr usage |
| 0x1 | MOVE.B | 88 | ✅ Safe | None | No get_n_addr usage |
| 0x2 | MOVE.L/MOVEA.L | 108 | ✅ Safe | None | No get_n_addr usage |
| 0x3 | MOVE.W/MOVEA.W | 108 | ✅ Safe | None | No get_n_addr usage |
| 0x4 | CLR/NEG/NBCD/NOT/MOVEM/LEA/PEA/JSR/JMP/RTS/LINK/UNLK/SWAP/EXT/TST/CHK | 197 | ✅ Pointer-safe | None | 9 handlers use safe readlong/writelong; MOVEM lifecycle and NBCD semantics are separately audited; other structural rows remain governed by the closure inventory |
| 0x5 | ADDQ/SUBQ/Scc/DBcc | 178 | ✅ Safe | None | No get_n_addr usage |
| 0x6 | Bcc/BSR/BRA | 42 | ✅ Safe | None | No get_n_addr usage |
| 0x7 | MOVEQ/EMUL_OP | 1 | ✅ Safe | None | No get_n_addr usage |
| 0x8 | OR/DIV/SBCD | 54 | ✅ Audited | None | Complete OR flag/no-flags/immediate/readable-and-writable-EA lifecycle; DIVS/DIVL zero/overflow and patched joins; SBCD exact correction, X/C/sticky-Z, A7 predecrement; no get_n_addr usage |
| 0x9 | SUB/SUBA/SUBX | 86 | ✅ Safe | None | No get_n_addr usage |
| 0xA | A-line traps | — | ✅ Interpreter | None | A-line handled by runtime helper |
| 0xB | CMP/CMPA/CMPM/EOR | 86 | ✅ Audited | None | CMP/CMPA/CMPM, complete EOR flag/no-flags/immediate/writable-EA lifecycles, and the reachable generic EOR emitter surface are separately audited; no get_n_addr usage |
| 0xC | AND/MUL/ABCD/EXG | 79 | ✅ Audited | None | AND complete flags/no-flags/immediate/EA lifecycle plus shared logical pre-write EA ownership; MULL explicit result ownership, full-product flags, aliases, and allocator lifetime; ABCD exact correction, X/C/sticky-Z, A7 predecrement; no get_n_addr usage |
| 0xD | ADD/ADDA/ADDX | 86 | ✅ Pointer-safe | None | No get_n_addr usage; `ADD` lifecycle is separately audited, while `ADDA`/`ADDX` remain governed by the closure inventory |
| 0xE | ASL/ASR/LSL/LSR/ROL/ROR/ROXL/ROXR | 36 | ✅ Audited | None | Register-count and fixed-memory flag/count/alias/X ownership complete; no get_n_addr usage |
| 0xF | MOVE16/FPU/cpSAVE/cpRESTORE | 27 | ✅ Safe | None | No get_n_addr usage |

## Fixes Applied

| Opcode | Instruction | Fix | Commit |
|--------|------------|-----|--------|
| 0x48d0-0x48f9 | MOVEM.L reg→mem (all EA modes) | writelong without mid_bswap | `0a98ff57` |
| 0x48e0 | MOVEM.L reg→-(An) predecrement | sub_l_ri + writelong | `0a98ff57` |
| 0x4cd0-0x4cfb | MOVEM.L mem→reg (all EA modes) | readlong + add_l_ri | `0a98ff57` |
| 0x4cd8 | MOVEM.L (An)+→reg postincrement | readlong + add_l_ri | `0a98ff57` |
| 0xf620 | MOVE16 (Ax)+,(Ay)+ | readlong + writelong_clobber | `c3ddf684` |
| 0xf600/f608 | MOVE16 other variants | readlong + writelong_clobber | `c3ddf684` |

## Remaining Barriers

| Barrier | Opcode | Reason | Removable? |
|---------|--------|--------|------------|
| EMUL_OP | 0x71xx | C-side state change via EmulOp() handler | No — architectural |

## Remaining Containment

| Range | Purpose | Removable? |
|-------|---------|------------|
| 0x04000000-0x0400ffff | Low ROM: $dd0 I/O, timer, early boot | Maybe — needs per-instruction analysis |
| 0x040b0000-0x040bffff | NuBus slot init: reads 0x50Fxxxxx hardware | No — unmapped hardware registers |

## Other Known Fixes (not 64-bit truncation)

| Fix | Commit | Description |
|-----|--------|-------------|
| Close configured AArch64 `i_FPP` generator lifecycle | current structural audit | Fail-closed reconciliation proves 8/8 top-level forms, exact set equality for 61 ordinary operation selectors across 22 semantic owners, 10 top-level FMOVE/FMOVEM/FMOVECR/control owners, maintained report/matrix pairs, and the accepted 904/904 plus 31/31 integration epoch; promotes exactly `i_FPP` without inheriting status onto compound MIDFUNC/raw or generic emitter rows; see `AARCH64_JIT_AUDIT_FPP_LIFECYCLE.md` |
| Audit AArch64 FMOVE signed-integer source MIDFUNC/raw lifecycle | current structural audit | Closes byte/word/long source conversion across exact Dn/fetched roots, 32-bit sign extension, `SCVTF_dw`, allocator lifetime, and D7->FP7 same-number maximum fields; adds a fail-closed 18-case integer subset while retaining the full 29-case source matrix; promotes exactly three MIDFUNC and three raw rows, leaves generic SXTB/SXTH/SBFM emitters and `i_FPP` unreviewed, and retains `SCVTF_dw` under its independent audit; see `AARCH64_JIT_AUDIT_FPP_FMOVE_INTEGER_SOURCE.md` |
| Retire definition-only legacy AArch64 FScc raw chain and residual low-byte emitters | current structural audit | Proves configured `i_FScc` uses the distinct `comp_fscc_opp` CMOV route, while zero-root `fp_fscc_ri` leads only to definition-only `raw_fp_fscc_ri`; pins the complete 15-case legacy switch and global 13/10 `CLEAR_LOW8_xx`/`SET_LOW8_xx` partition—11/10 in dead FScc plus two clear sites in unreachable CLR namesakes; classifies exactly those three rows unreachable while leaving live `i_FScc`, `CSETM_wc`, and `BFXIL_xxii` unreviewed; treats `fscc_false_byte` only as a live-route smoke check; see `AARCH64_JIT_AUDIT_FPP_LEGACY_FSCC_RAW.md` |
| Audit generic AArch64 FMSUB_dddd emitter API | current structural audit | Closes all 1,048,576 four-field encodings plus 400 native routes across true fused `Da−Dn×Dm` cancellation, directed rounding, signed overflow/underflow, invalid and three-position qNaN/SNaN ownership, 208 destination/source aliases, preload order, and external FP-state restoration; a later paired remainder audit retires both definition-only sites, so this remains historical direct evidence; see `AARCH64_JIT_AUDIT_FMSUB_EMITTER.md` |
| Audit generic AArch64 FDIV_ddd/FDIV_sss emitter pair | current structural audit | Separate binary64/binary32 probes close 65,536 encodings and 1,272 native routes across ±1/3 rounding, range, DZC/IOC, zero/infinity, NaNs, full single lanes, 584 aliases, every field and FP-state restoration; later lower-chain audits retire all retained 3/1 sites, so both direct audits remain historical evidence; see `AARCH64_JIT_AUDIT_FDIV_EMITTERS.md` |
| Retire paired serviced AArch64 FMOD/FREM lower chains and final divide/fused-subtract/round emitters | current structural audit | Proves configured FMOD/FREM service before native lowering, zero-root `fmod_rr`/`frem1_rr`, definition-only `raw_fmod_rr`/`raw_frem1_rr`, exact ordered divide→round→fused-subtract bodies, and aggregate FDIV/FMSUB/FRINTZ/FRINTA sites 3/2/2/1; classifies both raw wrappers plus `FDIV_ddd`, `FMSUB_dddd`, `FRINTZ_dd`, and `FRINTA_dd` unreachable, keeps `FRINTI_dd` audited/reachable at two sites, retains `i_FPP` unreviewed, and leaves repaired 31+1/33+1 exact-profile matrices as runtime owners; see `AARCH64_JIT_AUDIT_FPP_FMOD_BATCH.md` and `AARCH64_JIT_AUDIT_FPP_FREM_BATCH.md` |
| Retire serviced AArch64 FSGLDIV lower chain and sole binary32 divide emitter | current structural audit | Proves FSGLDIV services before acquisition, `fsgldiv_rr` has no configured caller, and `raw_fsgldiv_rr` is definition-only with exact ordered `FCVT_sd`→`FDIV_sss`→`FCVT_ds`; classifies the raw wrapper and sole-site `FDIV_sss` emitter unreachable; later paired remainder closure retires all three `FDIV_ddd` sites, while shared FCVT emitters remain audited/reachable at 7/6; keeps `i_FPP` unreviewed and leaves the 23+1 matrix as runtime owner; see `AARCH64_JIT_AUDIT_FPP_SGLDIV_BATCH.md` |
| Retire serviced AArch64 divide MIDFUNC/raw chain | current structural audit | Proves both configured `fdiv_rr` roots—FDIV/FSDIV/FDDIV and FSGLDIV—enter MPFR service before operand acquisition; with no other configured root/MIDFUNC caller classifies `fdiv_rr` and `raw_fdiv_rr` unreachable, guarded by exact two-root control-flow, edge, lower-chain, future-caller and variable-length two-pass profile checks; subsequent FSGLDIV and paired remainder closures retire `FDIV_sss` and all three `FDIV_ddd` sites; existing 37+3/23+1/31+1/33+1 matrices retain configured runtime ownership and `i_FPP` remains unreviewed; see `AARCH64_JIT_AUDIT_FPP_DIVIDE_BATCH.md` |
| Audit generic AArch64 FMUL_sss emitter API | current structural audit | Closes all 32,768 encodings plus 608 native binary32 routes across 24-bit halfway rounding, signed overflow/underflow, IXC/UFC/OFC, both zero×infinity IOC orders, left/right qNaN/SNaN, full-lane clearing, distinct-value Sn==Sm load ownership, 276 aliases, all S fields, and external FP-state restoration; a later lower-chain audit classified the definition-only wrapper and sole-site emitter unreachable, so this remains historical direct evidence; see `AARCH64_JIT_AUDIT_FMUL_S_EMITTER.md` |
| Audit generic AArch64 FMUL_ddd emitter API | current structural audit | Exhausted all 32,768 encodings plus 604 native semantic routes across rounding, signed range, exceptions, invalid/NaN handling, 272 aliases, all D fields and FP-state preservation; later configured-root audits retired both residual binary64 roots and the separate sole-site binary32 emitter, so this remains historical direct evidence; see `AARCH64_JIT_AUDIT_FMUL_D_EMITTER.md` |
| Retire serviced AArch64 FSGLMUL lower chain and sole binary32 multiply emitter | current structural audit | Proves FSGLMUL services before acquisition, `fsglmul_rr` has no configured caller, and `raw_fsglmul_rr` is definition-only with exact ordered `FCVT_sd`→`FMUL_sss`→`FCVT_ds`; classifies the raw wrapper and sole-site `FMUL_sss` emitter unreachable, retains shared FCVT emitters audited/reachable at 7/6, keeps `i_FPP` unreviewed, and leaves the 22+1 matrix as runtime owner; see `AARCH64_JIT_AUDIT_FPP_SGLMUL_BATCH.md` |
| Retire residual AArch64 native binary64 FMUL primitive chain | current structural audit | Proves both configured `fmul_rr` roots—FMUL/FSMUL/FDMUL and FSGLMUL—enter MPFR service before operand acquisition; with no other configured root/MIDFUNC caller classifies `fmul_rr`, `raw_fmul_rr`, and `FMUL_ddd` unreachable, guarded by exact two-root control-flow, edge, wrapper, emitter, future-caller and variable-length two-pass service-profile checks; existing 30+3 and 22+1 matrices retain configured runtime ownership; later lower-chain closure also retires `raw_fsglmul_rr` and `FMUL_sss`; this is retirement rather than native acceptance and `i_FPP` remains unreviewed; see `AARCH64_JIT_AUDIT_FPP_MUL_BATCH.md` |
| Audit generic AArch64 FSUB_ddd emitter API | current structural audit | Exhausted all 32,768 encodings plus 576 native semantic routes across operand order, rounding, signed cancellation, invalid/NaN handling, exceptions, 256 aliases, all D fields and FP-state preservation; a later configured-root audit retired the sole residual chain, so `FSUB_ddd` is now unreachable and this remains historical direct evidence; see `AARCH64_JIT_AUDIT_FSUB_EMITTER.md` |
| Retire residual AArch64 native FSUB primitive chain | current structural audit | Proves every FSUB/FSSUB/FDSUB selector enters MPFR service before operand acquisition and `fsub_rr`; configured AArch64 FCMP uses `fcompare_result_rr`, and with no other configured root/MIDFUNC caller classifies `fsub_rr`, `raw_fsub_rr`, and `FSUB_ddd` unreachable; exact branch/root/edge, wrapper, emitter, future-caller and variable-length two-pass service-profile checks guard the retirement; the existing 40 service plus three strict cases retain configured runtime ownership, this is retirement rather than native acceptance, and `i_FPP` remains unreviewed; see `AARCH64_JIT_AUDIT_FPP_SUB_BATCH.md` |
| Audit generic AArch64 FSQRT_dd emitter API | current structural audit | Exhausted all 1,024 encodings and 4,096 native field/mode routes across directed √2 rounding, exact roots and subnormal-root behavior, signed zero, invalid/NaN handling, IXC, 128 aliases and FP-state preservation; a later configured-root audit retired the sole residual chain, so `FSQRT_dd` is now unreachable and this remains historical direct evidence; see `AARCH64_JIT_AUDIT_FSQRT_EMITTER.md` |
| Retire residual AArch64 native FSQRT primitive chain | current structural audit | Proves every FSQRT/FSSQRT/FDSQRT selector enters MPFR service before operand acquisition and `fsqrt_rr`; with no other configured root/MIDFUNC caller, classifies `fsqrt_rr`, `raw_fsqrt_rr`, and `FSQRT_dd` unreachable, guarded by exact root/graph-edge, wrapper, emitter and future-caller checks; the existing 54 service plus three strict cases retain configured runtime ownership, this is retirement rather than native acceptance, and `i_FPP` remains unreviewed; see `AARCH64_JIT_AUDIT_FPP_SQRT_SUBTRANCHE.md` |
| Audit generic AArch64 FMOV_di immediate emitter API | current structural audit | Closes all 8,192 encodings and 32,768 native destination/immediate/mode routes across the complete 256-value `VFPExpandImm` domain, D0-D31, FPCR-mode independence, NZCV/FPCR/FPSR preservation, and external D8-D15/FP-state restoration; pins all five configured constant sites and promotes only `FMOV_di`, while wrappers, compound raw/MIDFUNC paths and `i_FPP` remain unreviewed; see `AARCH64_JIT_AUDIT_FMOV_DI_EMITTER.md` |
| Audit generic AArch64 FRINTA/FRINTI/FRINTZ emitter cluster | current structural audit | Closes all 3,072 encodings and 12,288 native field/mode routes across nearest-away, raw-FPCR-current and toward-zero rounding, signed zero, ties/subnormals, NaNs/IOC, non-X IXC silence, 384 aliases and FP-state preservation; later paired remainder closure retires sole-site `FRINTA_dd` and both `FRINTZ_dd` sites, so their direct evidence is historical while `FRINTI_dd` remains audited/reachable at two sites; see `AARCH64_JIT_AUDIT_FRINT_EMITTERS.md` |
| Retire definition-only AArch64 distinct-destination single-round raw wrapper | current structural audit | Proves already-unreachable `fmovs_rr` has no configured caller and `raw_fmovs_rr` only LOWFUNC/LENDFUNC references with exact `FCVT_sd`→`FCVT_ds` ordering; classifies only the raw wrapper unreachable, retains both shared emitters audited/reachable at exact 7/6 sites, and keeps `i_FPP` unreviewed; direct FCVT 2,048-word/256-route/64-alias evidence plus the live 21-case ordinary single-destination matrix own the lower semantics; see `AARCH64_JIT_AUDIT_FCVT_EMITTERS.md` |
| Repair ordinary-FMOVE source acceptance and retire definition-only split-double raw wrapper | current structural audit | Replaces the stale 43-case native aggregate with its current 29 native Dn/immediate owner plus the accepted 66+3 exact register-service owner; proves already-unreachable `fmov_d_rrr` has no configured caller and `raw_fmov_d_rrr` only LOWFUNC/LENDFUNC references with exact `BFI_xxii`→`FMOV_dx` ordering; classifies only the raw wrapper unreachable, retains `BFI_xxii` unreviewed/reachable and `FMOV_dx` audited/reachable at exact 2/6 sites, and keeps `i_FPP` unreviewed; see `AARCH64_JIT_AUDIT_FPP_FMOVE_SOURCE_SUBTRANCHE.md` |
| Retire definition-only AArch64 constant-10/100 raw wrappers | current structural audit | Proves `fmov_l_ri` has no configured root and its already-unreachable `fmov_d_ri_10`/`fmov_d_ri_100` children are the sole parent chain for definition-only `raw_fmov_d_ri_10`/`raw_fmov_d_ri_100`; classifies only the two raw wrappers unreachable, retains shared `FMOV_di`/`SCVTF_dw` audited/reachable at exact 5/6 source sites, leaves zero/one wrappers unchanged, and keeps `i_FPP` unreviewed; the existing FMOVECR 36+3 matrix owns configured runtime fidelity; see `AARCH64_JIT_AUDIT_FPP_FMOVECR_SUBTRANCHE.md` |
| Retire definition-only AArch64 cut-to-single raw wrapper | current structural audit | Proves `raw_fcuts_r` has only LOWFUNC/LENDFUNC references beneath already-unreachable `fcuts_r` after FSMOVE/FDMOVE service before operand acquisition; classifies only the raw wrapper unreachable, retains `FCVT_sd`/`FCVT_ds` audited/reachable at exact 7/6 source sites, and keeps `i_FPP` unreviewed; the existing explicit-move 13+2 matrix owns configured runtime fidelity; see `AARCH64_JIT_AUDIT_FPP_EXPLICIT_MOVE_SUBTRANCHE.md` |
| Retire definition-only AArch64 integral-rounding raw wrappers | current structural audit | Proves `raw_frndint_rr` and `raw_frndintz_rr` have only LOWFUNC/LENDFUNC references beneath already-unreachable `frndint_rr`/`frndintz_rr`, with FINTRZ's legacy call confined to inactive `USE_X86_FPUCW`; at landing time classified only the two raw wrappers unreachable; later paired remainder closure also retires `FRINTZ_dd` while `FRINTI_dd` remains audited/reachable at two sites; the existing FINT/FINTRZ 55+2 matrix owns configured runtime fidelity and `i_FPP` remains unreviewed; see `AARCH64_JIT_AUDIT_FPP_UNARY_DECOMPOSITION_BATCH.md` |
| Audit generic AArch64 SCVTF_dw emitter API | current structural audit | Closes all 1,024 encodings and 4,096 native field/mode routes across exact signed-int32→binary64 conversion, W31/WZR, poisoned X upper halves, D31, W0/W30, all callee-saved field numbers, 128 same-number cross-bank routes, FPCR-mode independence, no new FPSR exceptions, source/NZCV preservation, and external D8-D15/FP-state restoration; pins all six configured source sites and promotes only `SCVTF_dw`, while compound raw/MIDFUNC paths and `i_FPP` remain unreviewed; see `AARCH64_JIT_AUDIT_SCVTF_EMITTER.md` |
| Audit generic AArch64 FMOV_dx/FMOV_xd bit-transfer pair | current structural audit | Closes all 2,048 encodings and 2,048 native field routes across exact 64-bit X↔D copying, X31 XZR source/discard behavior, D31, X0/X30, all callee-saved field numbers, 64 same-number cross-bank routes, exact NaN/special payload images, source/NZCV/FPCR/FPSR preservation, and external D8-D15/FP-state restoration; pins all six/four configured source sites and promotes only `FMOV_dx`/`FMOV_xd`, while compound raw/MIDFUNC paths and `i_FPP` remain unreviewed; see `AARCH64_JIT_AUDIT_FMOV_DX_XD_EMITTERS.md` |
| Audit generic AArch64 FMOV_sw/FMOV_ws bit-transfer pair | current structural audit | Closes all 2,048 encodings and 2,048 native field routes across exact W↔S low-word copying, S upper-lane zeroing, W zero extension, W31 source/discard behavior, S31, W0/W30, all callee-saved field numbers, 64 same-number cross-bank routes, source/NZCV/FPCR/FPSR preservation, and external D8-D15/FP-state restoration; pins the sole configured raw composition for each API and promotes only `FMOV_sw`/`FMOV_ws`, while compound raw/MIDFUNC paths and `i_FPP` remain unreviewed; see `AARCH64_JIT_AUDIT_FMOV_SW_WS_EMITTERS.md` |
| Audit generic AArch64 FCVT_sd/FCVT_ds emitter pair | current structural audit | Closes all 2,048 encodings and 256 native conversions across exact widening, FPCR-directed narrowing, normal/subnormal/range edges, qNaN/SNaN quieting and IOC, IXC/UFC/OFC, 64 in-place aliases, source/NZCV preservation and external D8-D15/FPCR/FPSR restoration; pins all 13 configured source sites and the configured AArch64 single-memory call chain, with accepted 21/21 single-destination plus mechanically decoded 8/8 Dn/immediate and 3/3 memory single-import composition subsets; promotes only `FCVT_sd`/`FCVT_ds`, while compound raw/MIDFUNC paths and `i_FPP` remain unreviewed; see `AARCH64_JIT_AUDIT_FCVT_EMITTERS.md` |
| Audit generic AArch64 FCVTAS_wd emitter API | current structural audit | Closes all 1,024 W/D encodings and 256 native vectors across nearest-away ties, four FPCR modes, fractional IXC, invalid IOC, signed saturation, NaNs, W0/W30/W31 and D31 ownership, source/NZCV preservation, and external D8-D15/FPCR/FPSR restoration; pins its sole FRINTI→FCVTAS caller ordering and retains a mechanically selected 36/36 strict-native byte/word/long destination subset as composition evidence; promotes only `FCVTAS_wd`, while adjacent conversions and `i_FPP` remain unreviewed/unreachable as inventoried; see `AARCH64_JIT_AUDIT_FCVTAS_EMITTER.md` |
| Audit generic AArch64 FCMP emitter APIs | current structural audit | Closes `FCMP_dd` and `FCMP_d0` across all 1,056 encodings and 72 native vectors, including maximum register fields, self aliases, ordered/equal/unordered classes, qNaN/SNaN operands, exact NZCV, operand/FPCR preservation and FPSR IOC behavior; pins all five register-register and three register-zero production callers while retaining the 176-vector guest FCMP matrix as composition evidence; promotes only the two emitter rows and leaves `i_FPP` unreviewed; see `AARCH64_JIT_AUDIT_FCMP_EMITTERS.md` |
| Retire residual AArch64 native FADD primitive chain | current structural audit | Proves every FADD/FSADD/FDADD selector enters MPFR service before operand acquisition and `fadd_rr`; with no other configured root/MIDFUNC caller, classifies `fadd_rr`, `raw_fadd_rr`, and `FADD_ddd` unreachable, guarded by exact root/graph-edge, wrapper, and emitter checks; the existing 35 service plus three strict cases retain runtime ownership, this is retirement rather than native acceptance, and `i_FPP` remains unreviewed; see `AARCH64_JIT_AUDIT_FPP_ADD_BATCH.md` |
| Retire residual AArch64 native FABS/FNEG primitive chains | current structural audit | Proves every FABS/FSABS/FDABS and FNEG/FSNEG/FDNEG selector enters MPFR service before operand acquisition/native MIDFUNC dispatch; with no other configured caller, classifies `fabs_rr`/`fneg_rr`, `raw_fabs_rr`/`raw_fneg_rr`, and `FABS_dd`/`FNEG_dd` unreachable, guarded by positive ordered control-flow and caller-count checks; this is retirement rather than native acceptance and `i_FPP` remains unreviewed; see `AARCH64_JIT_AUDIT_FPP_SIGN_SUBTRANCHE.md` |
| Audit AArch64 FMOV binary64 primitive stack | current structural audit | Closes `FMOV_dd`, `raw_fmov_rr`, and `fmov_rr` after architectural extended register moves were removed from this path; exhausts all 1,024 D-register encodings and 10,240 native bit-copy vectors including 320 self aliases and ten exact bit classes, while proving source preservation, NZCV/FPCR/FPSR preservation, fixed D6/D7/D8-D15 ownership, dirty publication, and configured caller roles; `i_FPP` remains unreviewed; see `AARCH64_JIT_AUDIT_FMOV_PRIMITIVES.md` |
| Retire ordinary register-FMOVE copies to exact MPFR service | current structural audit | Prevents binary64-shadow copies from losing architectural extended significand/exponent and NaN metadata; exhausts all 64 FP0-FP7 source/destination pairs and self aliases plus single/double-FPCR low-bit witnesses through 66 service cases, with eight exact 80-bit classes, FPCR-independent copying, distinct-source preservation, signalling-NaN quieting/status, FPSR snapshot, integer state, exact fallback attribution and three strict rejections; no closure promotion because `fmov_rr` remains reachable outside this instruction path and `i_FPP` remains unreviewed; see `AARCH64_JIT_AUDIT_FPP_FMOVE_REGISTER_BATCH.md` |
| Retire ordinary-FMOVE binary64 destinations to exact MPFR service | current structural audit | Prevents stores from an already narrowed native shadow and restores instruction-local INEX2/OVFL/UNFL/SNAN publication; proves all FPCR directions, exact/inexact normal and range edges, signed specials, quiet/signalling NaN metadata without source mutation, FP0/FP7, ten writable EA classes, guarded bytes, writeback, CCR, exact fallback attribution and strict rejection through 28 service cases plus three strict cases; no closure promotion and `i_FPP` remains unreviewed; see `AARCH64_JIT_AUDIT_FPP_FMOVE_DOUBLE_DESTINATION_BATCH.md` |
| Audit dynamic-list FPP FMOVEM service | current structural audit | Confirms the existing pre-EA AArch64 fail-closed boundary and MPFR service for register-supplied lists; proves D0/D3/D7 selection, low-byte masking, all/sparse/empty lists, exact FP0/FP1/FP7 80-bit order, regular/predecrement masks, legal update modes, indexed and PC EAs, poisoned replay, guarded bytes, CCR, exact fallback attribution and strict rejection through 12 service cases plus three strict cases; evidence-only, no closure promotion, and `i_FPP` remains unreviewed; see `AARCH64_JIT_AUDIT_FPP_FMOVEM_DYNAMIC_BATCH.md` |
| Retire static-list FPP FMOVEM to exact MPFR service | current structural audit | Prevents binary64 shadows from being serialized as lossy extended frames; proves FP0/FP1/FP7 exact 80-bit order, regular/predecrement masks, legal update modes, indexed and PC EAs, poisoned replay, guarded bytes, CCR, exact fallback attribution and strict rejection through 10 service cases plus three strict cases; dynamic lists remain separate; no closure promotion; `i_FPP` remains unreviewed; see `AARCH64_JIT_AUDIT_FPP_FMOVEM_STATIC_BATCH.md` |
| Audit indexed FPP control-register memory transfers | current structural audit | Confirms FPCR/FPSR/FPIAR transfers across An/PC brief, full direct, preindexed and postindexed EAs; proves all/sparse masks, maximum word-index fields, independent pointer cells, guarded bytes, base/index preservation, represented FPCR/FPSR, full-width FPIAR, CCR, exact fallback attribution and strict rejection through 14 service cases plus three strict cases; no source repair or closure promotion; `i_FPP` remains unreviewed; see `AARCH64_JIT_AUDIT_FPP_CONTROL_MEMORY_INDEXED_BATCH.md` |
| Audit basic FPP control-register memory transfers | current structural audit | Confirms architectural FPCR/FPSR/FPIAR ordering across `(An)`, postincrement, predecrement, displacement, absolute, and PC-relative source EAs; proves all/sparse masks, guarded bytes, A7 frame updates, represented FPCR/FPSR, full-width FPIAR, CCR, exact fallback attribution and strict rejection through 15 service cases plus three strict cases; indexed forms remain separate; no source repair or closure promotion; `i_FPP` remains unreviewed; see `AARCH64_JIT_AUDIT_FPP_CONTROL_MEMORY_BASIC_BATCH.md` |
| Make direct FPP control-register transfers fail closed | current structural audit | Routes FPCR/FPSR/FPIAR Dn, An, and immediate forms through one pre-mutation semantic-service boundary; proves legal directions, D7/A7 fields, 68040 FPCR masking, FPSR represented-bit masking, full-width FPIAR, sequential visibility, CCR, exact native fallback attribution and four strict rejections through 11 service cases; memory forms remain separate; no closure promotion; `i_FPP` remains unreviewed; see `AARCH64_JIT_AUDIT_FPP_CONTROL_DIRECT_BATCH.md` |
| Repair FPP FSINCOS dual-result service | current structural audit | Retains one extended source, evaluates sine/cosine directly at FPCR width, preserves NaN sign, publishes sine-derived FPSR and enforces same-register sine-wins ordering across all eight cosine selectors; proves precision/direction, FP7 destinations, specials, NaNs, EA effects and FPSR through 18 service cases plus one strict rejection; no closure promotion; `i_FPP` remains unreviewed; see `AARCH64_JIT_AUDIT_FPP_SINCOS_BATCH.md` |
| Repair FPP FSGLMUL single-significand/extended-exponent service | current structural audit | Preserves arbitrary-precision factors and multiplies directly into a 24-bit significand under extended exponent limits, avoiding operand pre-rounding and double rounding; proves one-sided operands, an exact midpoint discriminator, directed modes, extended exponent range, signed specials, OPERR, NaNs, aliases, EA effects and FPSR through 22 service cases plus one strict rejection; no closure promotion; `i_FPP` remains unreviewed; see `AARCH64_JIT_AUDIT_FPP_SGLMUL_BATCH.md` |
| Repair FPP FSGLDIV single-significand/extended-exponent service | current structural audit | Preserves arbitrary-precision inputs and divides directly into a 24-bit significand under extended exponent limits, avoiding operand pre-rounding and double rounding; proves one-sided operands, a midpoint discriminator, directed modes, extended exponent range, specials, NaNs, aliases, EA effects and FPSR through 23 service cases plus one strict rejection; no closure promotion; `i_FPP` remains unreviewed; see `AARCH64_JIT_AUDIT_FPP_SGLDIV_BATCH.md` |
| Repair FPP FSCALE exponent and range service | current structural audit | Preserves extended operands, chops source toward zero independently of FPCR direction, post-rounds at FPCR width, forces huge finite factors relative to destination exponent, and retains signed invalid-NaN metadata; proves truncation boundaries, low bits, rounding, subnormals, range, specials, NaNs, aliases, EA effects and FPSR through 30 service cases plus one strict rejection; no closure promotion; `i_FPP` remains unreviewed; see `AARCH64_JIT_AUDIT_FPP_SCALE_BATCH.md` |
| Repair FPP FREM nearest-even remainder service | current structural audit | Uses exact extended operands, nearest-even quotient, FPCR result rounding/range, common binary NaN ownership and complete signed low-seven quotient publication; proves both half-way parity directions, quotient bit 6, signed specials, OPERR, aliases, EA effects and FPSR through 33 service cases plus one strict rejection; no closure promotion; `i_FPP` remains unreviewed; see `AARCH64_JIT_AUDIT_FPP_FREM_BATCH.md` |
| Repair FPP FSUB/FSSUB/FDSUB operand and result precision | current structural audit | Preserves extended operands and destination-minus-source order; evaluates ordinary FSUB directly at FPCR width; applies forced 24-/53-bit result range, cancellation handling and common binary NaN ownership; proves six one-sided operand-retention witnesses, signed zero, range, infinities, NaNs, aliases, EA effects and FPSR through 40 service cases plus three strict rejections; no closure promotion; `i_FPP` remains unreviewed; see `AARCH64_JIT_AUDIT_FPP_SUB_BATCH.md` |
| Repair FPP FMUL/FSMUL/FDMUL operand and result precision | current structural audit | Preserves extended factors; evaluates ordinary FMUL directly at FPCR width; applies forced 24-/53-bit result range and common binary NaN ownership; proves four one-sided operand-retention witnesses, signed specials, OPERR, range, aliases, EA effects and FPSR through 30 service cases plus three strict rejections; no guest-family closure promotion; later direct emitter audits close `FMUL_ddd` and `FMUL_sss`, while `i_FPP` remains unreviewed; see `AARCH64_JIT_AUDIT_FPP_MUL_BATCH.md`, `AARCH64_JIT_AUDIT_FMUL_D_EMITTER.md`, and `AARCH64_JIT_AUDIT_FMUL_S_EMITTER.md` |
| Repair FPP FADD/FSADD/FDADD operand and result precision | current structural audit | Preserves extended operands; evaluates ordinary FADD directly at FPCR result width; applies forced 24-/53-bit result precision/range; distinguishes exact cancellation from underflow; applies common binary NaN ownership; proves extended-only-bit witnesses, rounding, cancellation, range, infinities, aliases, EA effects and FPSR through 35 service cases plus three strict rejections; no row is promoted to audited, while the later configured-root addendum classifies `fadd_rr`, `raw_fadd_rr`, and `FADD_ddd` unreachable; `i_FPP` remains unreviewed; see `AARCH64_JIT_AUDIT_FPP_ADD_BATCH.md` |
| Repair FPP FMOD truncating remainder and quotient service | current structural audit | Removes incorrect native IEEE-remainder lowering on every host; preserves extended operands; computes truncating quotient sign/low-seven bits; post-rounds at FPCR width; applies special-value and common NaN ownership; proves `7 mod 4`, quotient wrap/sign, low-bit operands, range, specials, aliases, EA effects and FPSR through 31 service cases plus one strict rejection; no closure promotion; see `AARCH64_JIT_AUDIT_FPP_FMOD_BATCH.md` |
| Repair FPP FDIV/FSDIV/FDDIV operand, result and NaN ownership | current structural audit | Preserves both full extended operands; rounds ordinary FDIV only at FPCR result width; applies forced 24/53-bit result precision and exponent range; quiets signalling NaNs and applies architectural destination payload/sign precedence; proves rounding, range, zero/infinity status, dual NaNs, FP7 replay ownership, aliases, EA side effects and FPSR through 37 service cases plus three strict rejections; no closure promotion; see `AARCH64_JIT_AUDIT_FPP_DIVIDE_BATCH.md` |
| Repair FPP FCOSH/FACOS service and retire lossy native FCOS | current structural audit | Acquires extended sources and evaluates directly into FPCR-width MPFR results; replaces FCOS AArch64 binary64/native-libm lowering with pre-operand service; proves source-sensitive single/double results, directed rounding, signed zero, infinity/domain rules, NaN metadata, finite overflow, aliases and FPSR through 36 service cases plus three strict rejections; no closure promotion; see `AARCH64_JIT_AUDIT_FPP_COSH_ACOS_COS_BATCH.md` |
| Retire lossy native FPP FSIN/FETOX/FTWOTOX/FLOG2 to exact service | current structural audit | Replaces AArch64 binary64/host-libm lowering with pre-operand MPFR service; acquires extended sources and evaluates directly into FPCR-width results; proves directed single/double results, signed zero, infinity/domain rules, NaN metadata, finite overflow/underflow, minimum-extended FLOG2, aliases and FPSR through 49 service cases plus four strict rejections; no closure promotion; see `AARCH64_JIT_AUDIT_FPP_NATIVE_TRANSCENDENTAL_BATCH.md` |
| Repair FPP FTAN/FTENTOX/FLOGN/FLOG10 service precision | current structural audit | Keeps all four configured AArch64 MPFR boundaries; acquires extended sources and evaluates directly into separate FPCR-width results; proves source-sensitive single/double results, directed rounding, signed zero, infinity/domain rules, NaN metadata, finite/extended underflow, aliases and FPSR through 45 service cases plus four strict rejections; no closure promotion; see `AARCH64_JIT_AUDIT_FPP_TAN_EXP10_LOG_BATCH.md` |
| Repair FPP FATAN/FASIN/FATANH service precision and DZ status | current structural audit | Keeps all three configured AArch64 MPFR boundaries; acquires extended sources and evaluates directly into a separate FPCR-width result; repairs missing FATANH `±1` DZ publication; proves source-sensitive single/double results, directed rounding, signed zero, `±pi/2`, domains, NaN metadata, underflow, aliases and FPSR through 38 service cases plus three strict rejections; no closure promotion; see `AARCH64_JIT_AUDIT_FPP_INVERSE_BATCH.md` |
| Repair FPP FSINH/FLOGNP1/FETOXM1/FTANH service precision | current structural audit | Keeps all four configured AArch64 MPFR boundaries; acquires each source at extended precision/range and evaluates directly into a separate FPCR-width result to avoid source narrowing and transcendental double rounding; proves source-sensitive single/double results, directed rounding, signed zero, infinity/domain rules, NaN payload/quieting, extended-to-single underflow, aliases and FPSR through 48 service cases plus four strict rejections; no closure promotion; see `AARCH64_JIT_AUDIT_FPP_HYPERBOLIC_LOG1P_BATCH.md` |
| Repair FPP FINT/FINTRZ/FGETEXP/FGETMAN unary decomposition service | current structural audit | Keeps all four configured AArch64 MPFR boundaries; acquires each source at extended precision/range, performs exact integral or exponent/mantissa decomposition, then rounds/range-checks a separate FPCR-precision result; repairs extended-denormal import by one exponent, FGETEXP/FGETMAN infinity OPERR, and both helpers' MPFR/architectural NaN-sign metadata; proves immediate operation FPSR, exact 80-bit output, successor FNEG metadata consumption, precision modes, limits, specials, aliases and replay through 55+38 service cases plus four strict rejections; no closure promotion; see `AARCH64_JIT_AUDIT_FPP_UNARY_DECOMPOSITION_BATCH.md` |
| Retire lossy FPP FSQRT/FSSQRT/FDSQRT to exact MPFR service | current structural audit | Routes all three selectors through MPFR before binary64 operand acquisition and repairs shared forced-operation source narrowing by retaining extended source precision before 24/53-bit result rounding; proves signed zero, invalid negative input, NaN payload/quieting, extended range and low-bit fidelity, every FPCR precision/rounding class, forced single/double, FP7 self-alias, accrued FPSR and replay through 54 exact service cases plus three strict rejections; no closure promotion; see `AARCH64_JIT_AUDIT_FPP_SQRT_SUBTRANCHE.md` |
| Retire lossy FPP FABS/FNEG/FSABS/FSNEG/FDABS/FDNEG and repair mixed-mode FP ownership | current structural audit | Routes all six selectors through exact MPFR before binary64 operand acquisition; adds runtime dirty ownership so untouched shadows cannot narrow wider MPFR values or overwrite serviced FPSR, disassociates caller-clobbered allocator state before fallback C calls, and rematerialises integer CCR; proves 31 exact service cases plus six strict FP7 rejections; the new private NZCV-preserving call boundary is audited while no pre-existing row is promoted; see `AARCH64_JIT_AUDIT_FPP_SIGN_SUBTRANCHE.md` |
| Retire mixed-precision FPP FMOVECR dispatch to exact MPFR service | current structural audit | Replaces per-selector binary32/binary64/native-helper drift with one pre-selector MPFR boundary; proves all 22 defined selectors, powers through 10^4096, all π single/double rounding modes, representative undefined selectors, exact 80-bit/FPSR output through 36 service cases plus three strict FP7 rejections; no closure promotion; see `AARCH64_JIT_AUDIT_FPP_FMOVECR_SUBTRANCHE.md` |
| Retire unrounded FPP FSMOVE/FDMOVE copies to exact MPFR service | current structural audit | Separates explicit 24-/53-bit moves from ordinary native FMOVE, replacing the old unrounded binary64 copy with pre-operand service fallback; proves four single rounding directions, double halfway/overflow, signed specials and exact FPSR through 13 service cases plus two strict FP7 self-alias rejections; no closure promotion; see `AARCH64_JIT_AUDIT_FPP_EXPLICIT_MOVE_SUBTRANCHE.md` |
| Audit FPP ordinary packed-decimal FMOVE service boundary | current structural audit | Proves static/dynamic K factors, decimal rounding, signed exponent/mantissa, signed zero, and infinity through nine guarded MPFR service cases plus four strict source/destination rejections; source and destination decoders already return before EA side effects, so no implementation or closure promotion is needed; see `AARCH64_JIT_AUDIT_FPP_FMOVE_PACKED_FORMAT_SUBTRANCHE.md` |
| Retire lossy native FPP ordinary-FMOVE extended format | current structural audit | Rejects ordinary 80-bit source/destination compilation before EA side effects because the AArch64 binary64 shadow loses significand/exponent state and the old immediate-source route can dereference a host pointer as a guest vreg; proves eight byte-exact MPFR service round trips and four strict source/destination rejections; no closure promotion because extended helpers remain reachable from unaudited FPP compositions; see `AARCH64_JIT_AUDIT_FPP_FMOVE_EXTENDED_FORMAT_SUBTRANCHE.md` |
| Repair/audit native FPP ordinary-FMOVE extended destination EAs | current structural audit | Proves d16/indexed An and absolute short/long stores through 26 strict exact-native guarded-write vectors, including brief/full direct and indirect forms, maximum fields, and all integer registers live; rejects d16 PC, indexed PC, and immediate destinations through a 3-case fail-closed matrix; bounded no-promotion subtranche, so `i_FPP` remains unreviewed; see `AARCH64_JIT_AUDIT_FPP_FMOVE_DESTINATION_EXTENDED_EA_SUBTRANCHE.md` |
| Repair/audit native FPP ordinary-FMOVE IEEE-single destinations | current structural audit | Maps isolated AArch64 FCVT status to exact 68k SNAN/OVFL/UNFL/INEX2 and accrued fields, preserves guest NZCV/host FPSR plus MPFR NaN payload/sign, and proves all FPCR modes with normal/subnormal/range/special cases through 21 strict exact-native vectors; bounded no-promotion subtranche, so `i_FPP` remains unreviewed; see `AARCH64_JIT_AUDIT_FPP_FMOVE_SINGLE_DESTINATION_SUBTRANCHE.md` |
| Repair/audit native FPP ordinary-FMOVE basic destinations | current structural audit | Replaces invalid pointer-as-vreg destination conversion, implements MPFR-compatible byte/word/long rounding/saturation and FPSR OPERR/INEX publication, maps all four FPCR modes, preserves signed NaN across the MPFR/native boundary, and proves Dn plus `(An)`/postincrement/predecrement stores through 45 strict exact-native vectors; bounded no-promotion subtranche, so `i_FPP` remains unreviewed; see `AARCH64_JIT_AUDIT_FPP_FMOVE_DESTINATION_BASIC_SUBTRANCHE.md` |
| Repair/audit native FPP ordinary-FMOVE extended source EAs | current structural audit | Adds the missing PC-indexed compiler route and proves d16/indexed An, absolute short/long, d16/indexed PC, brief/full indexed and indirect forms through 39 strict exact-native value/FPSR vectors; bounded subtranche only, so `i_FPP` remains unreviewed; see `AARCH64_JIT_AUDIT_FPP_FMOVE_EXTENDED_EA_SUBTRANCHE.md` |
| Audit native FPP ordinary-FMOVE basic-memory sources | current structural audit | Proves `(An)`, `(An)+`, and `-(An)` across byte/word/long/single/double values, exact writeback, A7 byte geometry, and maximum A7/FP7 fields through 18 strict exact-native vectors; no implementation change or closure promotion, so `i_FPP` remains unreviewed; see `AARCH64_JIT_AUDIT_FPP_FMOVE_MEMORY_BASIC_SUBTRANCHE.md` |
| Repair native FPP ordinary-FMOVE sources | current structural audit | Replaces the invalid AArch64 `temp_fp` pointer-as-vreg compatibility route with typed byte/word/long/single conversions; the current fail-closed split proves 29 strict exact-native Dn/immediate value/FPSR routes plus 66 serviced and three strict FP-register routes across all 64 pairs and exact extended state; bounded subtranche only, so `i_FPP` remains unreviewed; see `AARCH64_JIT_AUDIT_FPP_FMOVE_SOURCE_SUBTRANCHE.md` |
| Repair native FPP FCMP/FTST condition results | current structural audit | Direct FCMP relation classification replaces subtraction, preserves equal signed-zero/infinity CCB distinctions and integer CCR, and passes 176 FCMP plus 128 FTST strict exact-native vectors with exact FPSR; bounded subtranche only, so `i_FPP` remains unreviewed; see `AARCH64_JIT_AUDIT_FPP_COMPARE_FTST_SUBTRANCHE.md` |
| Complete native FBcc generator lifecycle | current structural audit | All 16 FP predicates over positive/zero/negative/signed-NaN classes, word/long signed displacements, exact taken/not-taken successors, full integer CCR preservation, MPFR/native FPSR boundary synchronisation, and 160 strict exact-native vectors; see `AARCH64_JIT_AUDIT_FBCC_LIFECYCLE.md` |
| Complete EXT generator lifecycle | current structural audit | Six generated handlers split 3/3 across compiler tables, EXT.W byte-to-word upper-lane preservation, EXT.L word-to-long, EXTB.L byte-to-long, fixed logical flags, 16 exact-native vectors, and one scratch/source allocator witness; see `AARCH64_JIT_AUDIT_EXT_LIFECYCLE.md` |
| Complete EXG generator lifecycle | current structural audit | Six generated handlers split 3/3 across compiler tables, all Dn/Dn, An/An, and Dn/An forms, exact simultaneous exchange, self aliases, maximum fields, roundtrips, 12 exact-native vectors, and one temporary/source allocator witness; see `AARCH64_JIT_AUDIT_EXG_LIFECYCLE.md` |
| Complete CLR generator lifecycle | current structural audit | 48 generated handlers split 24/24 across compiler tables, byte/word/long Dn and all writable memory forms, exact upper-lane and fixed-flag semantics, store-before-flags ordering, 15 exact-native vectors, and one EA/zero allocator witness; six namesake MIDFUNCs remain unreachable; see `AARCH64_JIT_AUDIT_CLR_LIFECYCLE.md` |
| Complete Bcc generator lifecycle | current structural audit | 90 generated handlers split 45/45 across compiler tables, all fourteen conditional pairs plus BRA, byte/word/long signed target arithmetic, 34 exact-native vectors, two condition-translation boundaries, and exact CCR preservation; raw branch lowering remains separately unreviewed; see `AARCH64_JIT_AUDIT_BCC_LIFECYCLE.md` |
| Complete ADDA lifecycle and repaired source/writeback ownership | current structural audit | Four reachable no-flags MIDFUNC routes, 52 generated handlers, exact word sign-extension and long-width semantics, 27 exact-native plus two constant-fold vectors, and two allocator witnesses that reproduced both postincrement/source and destination/source collisions; see `AARCH64_JIT_AUDIT_ADDA_LIFECYCLE.md` |
| Generic SUB/SUBS emitter width/field/alias/NZCV contracts | current structural audit | Seven reachable AArch64 encoder APIs, 11 exact words, 70 direct native vectors, and 115 fail-closed raw caller checks; see `AARCH64_JIT_AUDIT_SUB_EMITTERS.md` |
| Complete SUB lifecycle and repaired destination-EA ownership | current structural audit | Twelve reachable SUB MIDFUNC routes, 208 generated handlers, nine readable and seven writable EA classes, 126 balanced writable-EA pins, 37 exact-native vectors, and the exact-native FLAGX/pre-write-EA collision which reproduced and verifies the repair; see `AARCH64_JIT_AUDIT_SUB_LIFECYCLE.md` |
| Complete OR lifecycle and source/destination EA ownership | current structural audit | Twelve reachable OR MIDFUNC routes, 156 generated handlers, nine readable and seven writable EA classes, 84 balanced writable-EA pins, 37 exact-native vectors, and two focused allocator collisions; see `AARCH64_JIT_AUDIT_OR_LIFECYCLE.md` |
| Generic EOR emitter encoding/bit/no-flags contracts | current structural audit | Four reachable callable AArch64 encoders plus `immOP_EOR`, 13 exact words, 22 direct native vectors, and 64 fail-closed raw source compositions; see `AARCH64_JIT_AUDIT_EOR_EMITTERS.md` |
| Complete EOR lifecycle and writable-EA ownership | current structural audit | Twelve reachable EOR MIDFUNC routes, 96 generated handlers, 84 balanced EOR memory-EA pins, 28 exact-native vectors, and two focused allocator collisions; see `AARCH64_JIT_AUDIT_EOR_LIFECYCLE.md` |
| Generic AND emitter width/field/no-flags contracts | current structural audit | Three reachable AArch64 encoder APIs, 9 exact words, 27 direct native vectors, and 83 fail-closed raw caller checks; see `AARCH64_JIT_AUDIT_AND_EMITTERS.md` |
| AND and shared logical pre-write EA lifecycle | current structural audit | Twelve reachable AND MIDFUNC routes, 84 AND and 252 shared OR/AND/EOR generated memory-EA pins, 34 complete AND vectors, two adjacent OR/EOR regressions, and two focused allocator collisions; see `AARCH64_JIT_AUDIT_AND_LIFECYCLE.md` |
| Generic ADD emitter width/field/no-flags contracts | current structural audit | Seven reachable AArch64 encoder APIs, 12 exact words, 46 direct native vectors, and 72 fail-closed raw caller checks; see `AARCH64_JIT_AUDIT_ADD_EMITTERS.md` |
| ADD pre-write EA and arithmetic lifecycle | current structural audit | Six shared MIDFUNC operand routes plus 126 generated memory-EA pins through ordered storage; 34 exact-native vectors and two focused allocator collisions; see `AARCH64_JIT_AUDIT_ADD_LIFECYCLE.md` |
| MOVEM cursor/base/extension lifecycle | current structural audit | Private load/store cursors, delayed update-mode publication, 68020+ base-in-mask predecrement semantics, exact PC-relative replay, special-memory service, and forced cursor/base allocator pressure; see `AARCH64_JIT_AUDIT_MOVEM_LIFECYCLE.md` |
| Long-multiply result/flag/allocator lifecycle | `4bb12cca` | Explicit Dl/Dh/source ownership, full-product N/Z/V, high-before-low alias ordering, and generator value locking under a forced S1-to-Dl collision; see `AARCH64_JIT_AUDIT_MULL_LIFECYCLE.md` |
| Fixed-memory shifts and ROX ownership | `01a04904` | `jff`/`jnf` memory-shift lifecycle, branchless C/V, and one RMW X binding under forced allocator pressure; see `AARCH64_JIT_AUDIT_MEMORY_SHIFTS_ROX.md` |
| Division zero/overflow lifecycle | `47ed4dea` | Signed 32/32 overflow, conditional result preservation, signed-overflow Z, aliases, and all 28 DIVL joins; see `AARCH64_JIT_AUDIT_DIVISION_LIFECYCLE.md` |
| BCD arithmetic/flag/A7 family | `1aeb577e` | `ABCD`/`SBCD`/`NBCD` share exact 68040 correction and X/C/sticky-Z handling; ordered byte predecrement uses `areg_byteinc[]`; see `AARCH64_JIT_AUDIT_BCD.md` |
| A-line trap L2 helper | `adc83002` | Runtime helper for A-line exception control flow |
| 64-bit PC_P truncation | `91f2e0f8` | add_l/sub_l_ri routes through arm_ADD_l for PC_P |
| Endblock pc_p store | `80afe33d` | Unconditional regs.pc_p store on hot chain |
| Spcflags mid-block PC | `c59cf7fe` | Full PC triple in spcflags cold path |
| Cross-block flag loss | `ffb1b731` | flush(save_regs=1) forces flags_are_important |
| tick_inhibit removal | `872ddd69` | Don't inhibit ticks during block tracing |
| Mid-block tick injection | `1f43f27f` | cpu_do_check_ticks every 64 compiled instructions |
| NuBus video probe patch | `ee27ef35` | ROM patch: jmp (a6) at 0xb27c |
| NuBus slot probe patch | `016d85ac` | ROM patch: beq→bra at 0xba0b0 |
